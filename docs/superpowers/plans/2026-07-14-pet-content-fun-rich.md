# 반려동물 궁합 콘텐츠 "재미 + 풍성" 개선 기획서

날짜: 2026-07-14
브랜치: feat/pet-resume
LLM: Gemini API (`gemini-2.5-flash`, temp 0.85) — Claude 아님
선행: Track A 콘텐츠 수술 완료 (1d1762c · c384014 · ea73ec1 · e61281b, prompts/history v0.3)
관련 계획서: `2026-07-14-pet-content-illustration.md` (Track B 아키타입 — 본 계획과 분기 조건 공유)

## 0. 목표 — 운영자 피드백 3가지

| # | 피드백 | 원인 실측 | 이 계획의 답 |
|---|---|---|---|
| 1 | 타이틀이 재미없다 | 라벨(서버 결정 문자열)이 설명조·밋밋 ("밥 주는 사람과 귀여운 갑", "까칠한 룸메이트"). 헤드라인은 신살 용어가 박힘 ("두부 홍염살에 네가 무릎 꿇는 관계" — 홍염살은 한글이라 한자 strip을 통과) | Task 1 라벨 전면 재작성 + Task 2 헤드라인 용어 금지 룰·QA 게이트 |
| 2 | 내용이 부실해 보임 | 판정 205~221자(4~6문장)로 얇진 않으나 장면·감각 디테일 부족 | Task 3 분량 상향 + 감각·장면 지시 |
| 3 | 첫인상 밀도 | 아코디언 6섹션 중 2개만 기본 펼침 (`initialExpandedCount={2}`) | Task 5 기본 펼침 확대 |

**전제(어기면 실패):** Track A 성과 — 트로프 0 · fabrication 0 · 명리 앵커 · QA 게이트 — 를 그대로 유지한 채 재미·풍성만 올린다. 점수·등급·분기 조건은 1글자도 안 바꾼다.

## 1. 현행 구조 실측 (2026-07-14 기준)

- **라벨**: `lib/pet-compat-scoring.ts` `pickLabelText`(l.347~383). 등급 × (sync / ruler / affectionGap=lover−loyalty / conflict) 분기. 기획 지시상 "12종"이나 실측은 **분기 endpoint 18개 · 고유 문자열 19개**(A등급 else가 sync≥75와 같은 문자열 재사용, D등급이 종 분기로 2문자열). 반환값은 `computePetCompatScores` → DB `label_text` → 결과 h1 · share 페이지 · 공유 텍스트에 그대로 노출.
- **★버그 발견**: S등급 `gap≤−25` 분기가 `"쭈가 너 없으면 안 되는 인연"` — 프롬프트 예시 펫 이름 "쭈"가 하드코딩돼 있어 **모든 펫에게 "쭈"로 표시**됨(pickLabelText는 petName을 안 받음). 이번 재작성에서 이름 없는 문자열로 교체하며 자연 해소.
- **헤드라인**: `lib/pet-compat.ts` 스키마 l.265 `headline`(25~40자), 역할 분리 룰 l.475~477(labelText가 안 말한 "가장 강한 명리 신호 1개 진단"). 한자 0개 룰(l.308)은 있으나 **한글 신살·12운성·십성 용어 금지 룰이 없음** → "홍염살" 노출 사고.
- **판정 분량**: 스키마 지시 ownerVerdict·petVerdict 4~6문장, simulations prediction 4~6문장, futureLine 3~4문장. `maxOutputTokens 8192`.
- **QA 게이트**: `lib/pet-compat-postprocess.ts` `validatePetCompatResult` — 금지어(FORBIDDEN)·TROPE_BLACKLIST(16종)·12운성 불일치·의료 증상 검출 → 위반 시 재생성 1회(`runPetCompatAnalysis` l.515~537). **길이 검사는 없음** → 분량 상향으로 게이트가 깨질 요소 없음(§5에서 재확인).
- **프론트**: `app/pet/result/PetResultClient.tsx` l.208 `initialExpandedCount={2}` — 섹션 6개(보호자·펫 판정 + 시뮬 3 + 타임라인) 중 2개만 펼침.
- **Track B 결합점**: 일러스트 계획서 Task B1이 `pickLabelText` → `pickLabelAndArchetype` 확장 예정. 아키타입은 **분기 조건**에 1:1 매핑(문자열이 아니라) → **문자열만 바꾸고 분기 조건·순서를 유지하면 Track B와 무충돌**.

## 2. Task 1 — 라벨 전면 재작성 (분기 19문자열)

**Files:** Modify `lib/pet-compat-scoring.ts`(pickLabelText 반환 문자열만), `lib/pet-compat.ts`(시스템 프롬프트 [등급→라벨 매핑 룰] l.204~214의 예시 문자열 동기화)

**톤 가이드 (라벨 공통 룰):**
- 귀엽게 놀리기 + 보호자 직설(놀림의 화살이 보호자를 향해도 됨 — 두루미 결)
- 명리 용어·한자 0개 (label.text는 캡처·공유용 즉시 가독 대상 — 기존 l.308 룰 그대로)
- "운명" 및 절대 금지 단어(100%/절대/영원히/무조건/반드시/정답) 금지 — 기존 룰 유지
- 밈처럼 회자되게: 일상어 반전 한 번(무급인데 평생직장), 구체 장면 하나(현관 소리만 기다리는)
- 시니어(35–54 여성) 시청자 — MZ 전용 슬랭(읽씹·킹받네 류) 금지. 츤데레·껌딱지·셔틀처럼 전 연령 통용어만
- 펫 이름·특정 이름 금지(쭈 버그 재발 방지). "얘/이 아이/너"만
- 등급 내 의미(누가 우위·누가 매달림·갈등)와 분기 조건은 그대로 — 문자열 교체만

**재작성 표 (분기 순서 = 코드 순서, 조건 무변경):**

| # | 등급 | 분기 (불변) | 현재 | 제안 | 유지한 의미 |
|---|---|---|---|---|---|
| 1 | S | sync≥85 && \|gap\|≤15 | 사주가 맞춘 찰떡 인연 | **전생에 한 이불 쓰던 인연** | 최고 조화·쌍방 |
| 2 | S | gap≥25 | 네가 더 매달리는 팔자의 인연 | **찰떡인데 더 빠진 쪽은 너** | 좋은 관계·보호자 일방 |
| 3 | S | gap≤−25 | 쭈가 너 없으면 안 되는 인연 (★쭈 하드코딩 버그) | **공식 인증 껌딱지 인연** | 좋은 관계·펫 일방 |
| 4 | S | else | 사주가 맞춘 인연 | **팔자가 먼저 알아본 인연** | 좋은 관계 일반 |
| 5 | A | sync≥75 (else와 공유) | 찰떡 같은 콤비 | **손발 척척 환상의 복식조** | 호흡 좋음 |
| 6 | A | gap≥30 | 네 사랑이 더 큰 콤비 | **애정 지분은 네가 51%** | 보호자 우세 애정 |
| 7 | A | gap≤−30 | 이 아이가 너에게 헌신하는 콤비 | **너에게 올인한 순정파** | 펫 우세 애정 |
| 8 | A | conflict≥30 | 서로 좋아하지만 둘 다 정상은 아님 | **서로 좋아 죽는데 둘 다 유난함** | 애정+마찰 |
| 9 | B | ruler≥70 && gap≥20 | 밥 주는 사람과 귀여운 갑 | **간식 셔틀과 네 발 상전** | 펫 우위+보호자 매달림 |
| 10 | B | ruler≤30 | 사랑인 줄 알았는데 운영 계약 | **이 집 결재권자는 너** | 보호자 우위 |
| 11 | B | gap≥35 | 혼자 일방통행하는 사랑 | **너만 애가 타는 짝사랑** | 보호자 일방 |
| 12 | B | gap≤−35 | 그 사이 더 많이 사랑하는 쪽은 아이야 | **현관 소리만 기다리는 순애보** | 펫 일방 |
| 13 | B | else | 까칠한 룸메이트 | **츤데레 룸메이트** | 중간·소마찰 |
| 14 | C | ruler≥65 && gap≥20 | 집안 실세와 월급 없는 운영진 | **무급인데 평생직장** | 펫 군림+보호자 헌신 |
| 15 | C | conflict≥50 | 어긋난 박자, 그래도 가족 | **투닥거려도 결국 한솥밥** | 갈등+유대 |
| 16 | C | gap≥30 | 네 짝사랑이 그리는 관계 | **들이대는 너, 한 발 빼는 얘** | 보호자 일방(냉담 응답) |
| 17 | C | else | 사주는 다르지만 팔자가 묶었어 | **극과 극인데 한 지붕 아래** | 상이·동거 |
| 18 | D | species=cat | 사주가 멀리 본 묘연 | **천천히 스며드는 묘연** | 거리감+희망 (묘연 유지) |
| 19 | D | species=dog | 사주가 멀리 본 인연 | **천천히 스며드는 인연** | 거리감+희망 (인연 유지) |

**표 검수 노트:**
- #9(B 펫우위, 애교 갑)와 #14(C 펫우위, 마찰 동반)는 같은 축이라 단어 겹침 주의 — 상전(#9)/평생직장(#14)로 분리했다. #11(B 짝사랑)/#16(C 짝사랑)도 애타는(#11)/한 발 빼는(#16)로 온도차를 뒀다(C가 더 시니컬 — 등급 하강과 톤 일치).
- #6 "51%"는 숫자지만 금지 단어("100%") 아님. 캡처 시 재미 포인트.
- 아키타입 매핑 영향 0: Track B 표(B1 Step 2)는 분기 조건에 매핑 — #2=OWNER_DEVOTION, #3=PET_DEVOTION, #9·#14=PET_THRONE… 전부 그대로.
- 시스템 프롬프트 l.207~211의 라벨 예시("까칠한 룸메이트" 등)와 l.336("사주가 맞춘 인연" 예시)도 새 문자열로 동기화. 서버가 어차피 덮어쓰지만(l.506) LLM이 옛 라벨 어휘를 본문에 흉내 내는 누수를 막는다.
- l.213~214의 "ruler≥70이면 label에 실세/폐하 코드" 룰은 라벨이 서버 결정이 된 v0.3 이후 사문(死文) — 이번에 삭제(정리).

## 3. Task 2 — 헤드라인·finalLine 용어 금지 룰 강화

**Files:** Modify `lib/pet-compat.ts`(시스템 프롬프트 [절대 출력 규칙] l.304~312 + 역할 분리 블록 l.475~477), `lib/pet-compat-postprocess.ts`(validate 확장 — Task 4와 함께)

**룰 (프롬프트 추가 문안 요지):**
- headline·finalLine·label.text에는 **한자 0개에 더해 명리 용어 0개** — 신살 이름(홍염살·도화살·역마살·괴강·백호살·천을귀인·공망 등), 12운성 이름, 십성 이름(관성·인성·식상·비겁·재성 및 하위 10종), 신강/신약, 합·충·형·원진·삼합·육합·방합, 오행 낱말( "금 기운" 류) 전부 금지. 용어를 밝히는 자리는 본문(petVerdict·ownerVerdict) **가장 강한 신살 1개**뿐(기존 l.309 룰과 일관).
- headline = 25~40자, **명리 신호를 "행동·장면"으로 번역한 한 방 카피**. labelText와 다른 각도·다른 단어(기존 룰 유지).
- ✅ 좋은 예 (홍염살 → 행동 번역): "눈 한 번 마주치면 네 지갑이 열리는 사이" / (일지 충 → 리듬): "네가 자려고 눕는 순간 얘 하루가 시작된다"
- ❌ 나쁜 예: "두부 홍염살에 네가 무릎 꿇는 관계" (용어 노출) / "일지 삼합이 만든 환상 호흡" (용어) / labelText 단어 재탕
- finalLine(25~50자)도 같은 용어 금지 적용 — 감정 마무리 한 줄, 공유 텍스트에 labelText와 함께 찍히므로(PetResultClient `handleShare`) 둘의 단어 중복 금지 룰 유지.

## 4. Task 3 — 판정·시뮬 풍성화 (분량 + 장면 밀도)

**Files:** Modify `lib/pet-compat.ts`(스키마 분량 지시 l.284~292 + [명리 앵커] l.132~139에 디테일 지시 추가)

**변경 요지 (지시 문구 구체안):**
- `ownerVerdict`: 4~6문장 → **5~7문장**. "판정 안에 이 보호자 사주에서 나온 구체 생활 장면 1개(시간대·장소가 보이는)를 포함하라."
- `petVerdict`: 4~6문장 → **5~7문장**. 기존 신살 앵커 리드(첫 두 문장) 유지 + "이 펫의 품종·나이가 보이는 묘사 1개, 오감 디테일(소리·촉감·냄새 등) 1개 이상을 넣어라."
- `simulations[].prediction`: 4~6문장 → **5~7문장**. "각 시뮬에 오감 디테일 1개 + 관찰 가능한 구체 행동 1개 이상. 결말 한 문장은 예측 단정형으로(두루미 결)." 도입부 3구조 변형 룰(l.251~257) 유지.
- `futureLine`: 3~4문장 유지(신파 방지 — 늘리지 않는다).
- manual 필드는 스펙시트 UI라 분량 유지(늘리면 표가 깨짐).
- **중복 가드 유지**: "같은 표현 두 번 금지"(l.314)·트로프 금지가 길어진 본문에도 그대로 적용됨을 앵커 블록에 재명시("길어진 만큼 채우기용 상투구가 생기면 실패").
- **토큰 여유 점검**: 상향분은 필드당 1~2문장(전체 +25% 내외). 현행 `maxOutputTokens: 8192`로 충분하나, dev-test에서 JSON 잘림(파싱 실패) 1건이라도 나오면 **10240으로 상향**을 같은 커밋에 포함(사전 확정 — 렌더 전 파라미터 확정 원칙).

## 5. Task 4 — QA 게이트·트로프 재점검

**Files:** Modify `lib/pet-compat-postprocess.ts`

1. **헤드라인 용어 게이트 신설**: `validatePetCompatResult`에 headline·finalLine 전용 검사 추가 — 신살·12운성·십성·합충 용어 리스트 매칭 시 `헤드라인 용어 "○○"` 위반 반환 → 기존 재생성 루프에 자동 편입(runPetCompatAnalysis 변경 불필요).
   - 매칭은 **긴 고유 용어만**(홍염살·도화살·역마살·천을귀인·괴강·백호살·공망·삼합·육합·방합·원진·신강·신약 + 십성 10종 + 12운성 2글자 5종). 한 글자 12운성(쇠·병·사·묘·태·양)은 일반어 오탐이 커서 headline 검사에서 제외(본문 12운성 검사 l.69~78의 문맥 매칭 방식 유지).
   - ★**heredoc CJK 사고 예방**: 용어 리스트·정규식은 반드시 **TS 파일 직접 편집**(Edit 툴)으로 넣는다. bash heredoc으로 한글 정규식 주입 금지. 검증 스크립트에서 정규식이 필요하면 `\uXXXX` 이스케이프 또는 per-field `includes` 비교로.
2. **QA 게이트 길이 내성 확인**: validate에 길이 상한 검사 없음 → 분량 상향으로 깨질 규칙 없음(확인 완료). 단 본문이 길수록 트로프·금지어 우발 포함 확률이 오르므로 재생성 1회 루프의 잔존 위반 로그(`[PET_COMPAT][QA] 잔존 위반`)를 dev-test에서 반드시 열람.
3. **새 위트가 새 클리셰가 되지 않게(다양성 확보)**:
   - 라벨의 핵심 어휘(껌딱지·상전·셔틀·순정파·순애보·복식조·평생직장·츤데레)를 시스템 프롬프트에 "labelText에 이미 쓴 단어 — headline·본문에서 재사용 금지" 목록이 아니라 **기존 룰(label/headline/finalLine 단어 재사용 금지, l.475)로 커버됨을 확인**하고, 본문(petVerdict 등)에서도 labelText 핵심 단어를 리드로 복사하지 말라는 1줄을 앵커 블록에 추가.
   - TROPE_BLACKLIST **선제 추가는 하지 않는다**(새 표현이 실제로 반복되는지 실측 전 금지 목록을 불리면 표현 공간만 좁아짐 — Track A 교훈은 "실측된 상투구만 금지"). 대신 §7 검증에서 다펫 4건 교차 비교로 반복 표현 후보를 기록하고, 반복 실측 시 후속 커밋으로 추가.
   - 라벨 문자열은 validate 검사 제외(l.20 주석) 유지 — 서버 결정 문자열이라 대상 아님. "집안 실세"가 라벨에서 사라지므로 블랙리스트와 라벨의 어색한 동거도 해소.
4. **가이드 문구 정리(소)**: `buildRelationSignalBlock` l.403 "집안 실세로 그려도 됨"이 블랙리스트 용어를 가이드에 노출 — LLM이 따라 쓰면 QA 재생성 유발. "왕좌·상전 프레임으로 그려도 됨(단 '집안 실세' 표현은 금지)"으로 교체. UI 섹션 제목 "집안 실세"(PetResultClient l.195)는 서버 텍스트가 아니라 무관 — 건드리지 않는다.

## 6. Task 5 — 프론트 기본 펼침 (첫인상 밀도)

**Files:** Modify `app/pet/result/PetResultClient.tsx` l.208

- `initialExpandedCount={2}` → **`{6}` (전부 펼침)** 권장. 근거: (a) 운영자 지적이 "부실해 보임" — 실제 분량은 충분한데 접힘이 밀도를 숨김, (b) 시니어 시청자 원칙(한 번에 보이게 > 인터랙션 요구), (c) 아코디언 접기 기능은 남아 있어 정보 과부하 시 사용자가 접을 수 있음.
- 대안: `{3}`(판정 2 + 시뮬 1). 운영자가 스크롤 길이를 우려하면 이걸로 — 구현 전 스크린샷 2안 비교로 운영자 선택 1회(작은 변경이라 dev 데모 페이지 `PetResultBody` mock으로 즉시 비교 가능).
- share 페이지는 SectionList 미사용(영향 0).

## 7. Task 6 — 검증 플랜 (실행 순서 포함 태스크 분해)

| 순서 | Task | 파일 | 검증 |
|---|---|---|---|
| 1 | T1 라벨 19문자열 교체 (+쭈 버그 해소, 사문 룰 삭제) | pet-compat-scoring.ts · pet-compat.ts(매핑 룰 동기화) | `npx tsx scripts/pet-compat-grade-dist.mts` — 등급 분포 **완전 동일**(분기 무변경 증명) + 신 라벨 19종 전수 출현 확인. `npx next build` |
| 2 | T2 헤드라인 용어 금지 룰 | pet-compat.ts | 프롬프트 diff 리뷰 (실효는 4~5단계에서) |
| 3 | T3 풍성화 지시 (+토큰 사전 확정) | pet-compat.ts | 〃 |
| 4 | T4 QA 게이트 확장 | pet-compat-postprocess.ts | 단위: 위반 픽스처(용어 박은 headline)로 validate 검출 확인 — `npx tsx`로. ★CJK는 파일 직접 편집, heredoc 금지 |
| 5 | 통합 dev-test | `PORT=3005 npm run dev` → POST `/api/pet-compat/dev-test` | 다펫 4건(나비·보리·두부 + 신규 1, 개2/고양이2·등급 산개). 무료(텍스트만) |
| 6 | T5 기본 펼침 | PetResultClient.tsx | mock 데모 페이지 스크린샷 2안 → 운영자 선택 |
| 7 | T7 프롬프트 히스토리 | prompts/history | v0.4 파일 존재 + diff 표 |

**통합 dev-test 합격 기준 (5단계):**
- 라벨: 응답 label.text가 신 문자열, "쭈" 미출현
- 헤드라인·finalLine: 명리 용어 0 · 한자 0 · labelText 단어 중복 0
- 분량: petVerdict·ownerVerdict·시뮬 각 5~7문장 실측 (문장 수 카운트)
- 트로프 0 · 금지어 0 · 12운성 fabrication 0 · spec 정확 (Track A 회귀 없음 — `[PET_COMPAT][QA] 잔존 위반` 로그 0 확인)
- 4건 교차: 같은 비유·같은 서사 아크 반복 없음(새 클리셰 후보 기록)
- 재미 체감: 최종 판정은 운영자 — 4건 결과 텍스트를 정리해 보고
- 라벨 12분기 커버는 LLM 호출 없이 grade-dist 스크립트(1단계)가 담당 — dev-test로 19분기 전부 뽑으려 유료급 반복 호출하지 않는다(텍스트라 무료지만 시간 낭비)

## 8. Task 7 — prompts/history v0.3 → v0.4

`prompts/history/pet-compat_v0.4_fun-rich_2026-07-14.md` 생성 (CLAUDE.md 룰: 버전별 저장 + 이전 버전과 차이 비교):
- v0.3 대비 diff 표: ① 라벨 19문자열 교체(+쭈 버그) ② 헤드라인·finalLine 명리 용어 0 룰 + QA 게이트 ③ 판정·시뮬 5~7문장 + 오감/장면 디테일 ④ 매핑 룰 동기화·사문 삭제 ⑤ (조건부) maxOutputTokens 8192→10240
- 왜 바꿨나: 운영자 피드백 3종 인용 + dev-test 실측 결과 첨부

## 9. 회귀 불변 (어기면 실패)

| 불변 | 근거 |
|---|---|
| Track A 성과: 트로프 0·fabrication 0·명리 앵커·QA 재생성 루프 | 본 계획은 룰 추가·문자열 교체만, 기존 게이트 삭제 없음 |
| `pickLabelText` 분기 조건·순서·시그니처 | Track B `pickLabelAndArchetype`가 분기에 1:1 매핑 — 문자열만 교체 |
| share 페이지·DB `label_text` | 스키마 무변경. 기존 row는 옛 라벨 유지(정상 — 재분석 시 새 라벨), 마이그레이션 불필요 |
| `PET_COMPAT_SCORING_VERSION = 4` | 점수 로직 무변경. 라벨은 분석 시점 파생 표시값 |
| Phase 2 레이아웃 (PetResultBody 구조·share 시각 동기화) | 프론트 변경은 initialExpandedCount 1곳뿐 |
| 종 어휘 가드·D등급 disclaimer·"운명" 금지 | 라벨 신안 전수 검수 완료(§2 표) — 용어 0·금지어 0·묘연/인연 종 분기 유지 |

**롤백**: 태스크별 독립 커밋 → `git revert` 단위 회수. 라벨은 T1 커밋 1개 revert로 원복(DB에 이미 저장된 신 라벨 row는 표시 문자열일 뿐이라 무해). QA 확장은 validate 내 검사 블록 삭제로 무력화. 프롬프트는 history v0.3 파일로 즉시 복원.

## 10. 첫 착수

**T1 (라벨 교체)** — 의존성 0, 효과 최대(운영자 지적 1순위), grade-dist 스크립트로 즉시 결정론 검증 가능. T2~T4는 프롬프트·QA가 맞물려 한 흐름으로 이어서, T5는 운영자 스크린샷 선택 후 마지막에.
